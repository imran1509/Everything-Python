# Replace the double space from problem 3 with single spaces.

# Write a program to detect double space in a string

name = "Imran is a good  developer"
print(name.replace("  ", " "))
print(name) # strings are immutable which means you cannot change them by running functions on them
